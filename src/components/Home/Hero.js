// Media
import hero from "../../assets/img/hero.png";

const Hero = () => {
  return (
    <div className="hero-container">
      <div></div>
      <img src={hero} alt={hero} />
    </div>
  );
};

export default Hero;
